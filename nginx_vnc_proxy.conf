# VNC Nginx Proxy Configuration - Updated
# Add this OUTSIDE of server block (at http level)

map $uri $vnc_port {
    ~^/vnc/(\d+)/ $1;
}

# Add this INSIDE your server { } block
location ~ ^/vnc/(\d+)/ {
    rewrite ^/vnc/\d+/(.*)$ /$1 break;
    proxy_pass http://127.0.0.1:$vnc_port;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_read_timeout 86400;
    proxy_buffering off;
}

# --- ALTERNATIVE: SIMPLER FIXED PORT ---
# If the above doesn't work, use fixed port mapping:

# location /vnc/ {
#     proxy_pass http://127.0.0.1:6080/;
#     proxy_http_version 1.1;
#     proxy_set_header Upgrade $http_upgrade;
#     proxy_set_header Connection "upgrade";
#     proxy_read_timeout 86400;
# }

# --- HOW TO USE ---
# 1. sudo nano /etc/nginx/sites-available/tool.1amsleep.xyz
# 2. Add "map" block BEFORE server { } block
# 3. Add "location" block INSIDE server { } block  
# 4. sudo nginx -t
# 5. sudo systemctl reload nginx
